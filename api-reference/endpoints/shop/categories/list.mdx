---
title: "List Categories"
api: "GET /{locale}/shop/categories"
description: "Get available offer/product categories"
---

# List Categories

Get a list of available categories for offers and products.

## Endpoint

```
GET /{locale}/shop/categories
```

## Authentication

<ParamField header="X-API-Key" type="string" required>
  Partner API key
</ParamField>

<ParamField header="X-API-Secret" type="string" required>
  Partner API secret
</ParamField>

## Response

<ResponseField name="data" type="array">
  Array of category objects
  
  <Expandable title="category object">
    <ResponseField name="id" type="integer">
      Category ID
    </ResponseField>
    
    <ResponseField name="name" type="object">
      Localized name (`lt`, `en`)
    </ResponseField>
    
    <ResponseField name="slug" type="string">
      URL-friendly identifier
    </ResponseField>
    
    <ResponseField name="parent_id" type="integer">
      Parent category ID (null for root categories)
    </ResponseField>
    
    <ResponseField name="children" type="array">
      Nested child categories
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://staging-api.loyalty.lt/en/shop/categories" \
  -H "X-API-Key: your_api_key" \
  -H "X-API-Secret: your_api_secret"
```

</RequestExample>

<ResponseExample>

```json Success Response (200)
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": {
        "lt": "Gerimai",
        "en": "Beverages"
      },
      "slug": "beverages",
      "parent_id": null,
      "children": [
        {
          "id": 2,
          "name": {
            "lt": "Kava",
            "en": "Coffee"
          },
          "slug": "coffee",
          "parent_id": 1
        },
        {
          "id": 3,
          "name": {
            "lt": "Arbata",
            "en": "Tea"
          },
          "slug": "tea",
          "parent_id": 1
        }
      ]
    }
  ]
}
```

</ResponseExample>

