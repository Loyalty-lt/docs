# Dependencies
/node_modules

# Docusaurus build output and caches
/build
/build.new
/build.old
/.docusaurus
.cache-loader

# pm2 logs (see ecosystem.config.cjs)
/logs

# Generated by `npm run gen-api` from static/api-reference/openapi.json.
# Regenerated on every build, so it does not belong in git.
/docs/api-explorer

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
